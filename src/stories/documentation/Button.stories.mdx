

<!--- Checkbox.stories.mdx --->

import { Fragment } from "react";
import { Meta, Story, Canvas } from '@storybook/addon-docs';

import { StoryProvider, Text, Button} from "../theme";
import { Alert } from "../../Alert";
import { css } from "tss-react/@emotion/css";

<Meta title="Documentation/Button" component={Button} />

# Checkbox

This is a demo of `<Alert />`, components.

<Canvas>
	<Story name="foo bar">
		<StoryProvider dark={false}>
			<Alert severity="success"><Text typo="body 1">This is a success</Text></Alert>
			<Alert severity="warning"><Text typo="body 1">This is a warning</Text></Alert>
			<Alert severity="info"><Text typo="body 1">This is an info</Text></Alert>
			<Alert severity="error"><Text typo="body 1">This is an error</Text></Alert>
		</StoryProvider>
		<div className={css({ "height": 20 })} />
		<StoryProvider dark={true}>
			<Alert severity="success"><Text typo="body 1">This is a success</Text></Alert>
			<Alert severity="warning"><Text typo="body 1">This is a warning</Text></Alert>
			<Alert severity="info"><Text typo="body 1">This is an info</Text></Alert>
			<Alert severity="error"><Text typo="body 1">This is an error</Text></Alert>
		</StoryProvider>
	</Story>
</Canvas>

# Buttons

This is a demo of the `<Button />` component.

```tsx
function MyButton(){
	return <h1>Hello World</h1>;
}
```
<Canvas>
	<Story name="All buttons 2">
		{[true, false].map(dark=>
			<Fragment key={dark}>
				<StoryProvider dark={dark}>
					<Button href="#">Button</Button>
					<Button href="#" startIcon="help" >Button with icon</Button>
				</StoryProvider>
			</Fragment>
		)}
	</Story>
</Canvas>

Something else

