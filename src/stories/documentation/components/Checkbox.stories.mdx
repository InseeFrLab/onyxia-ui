<!--- Checkbox.stories.mdx 
--->

import { Fragment } from "react";
import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { ThemeProvider } from "../../theme";
import { Text } from "../../../Text";
import { Checkbox } from "../../../Checkbox";
import { css } from "@emotion/css";
import { useDarkMode } from "storybook-dark-mode";

<Meta
    title="documentation/Components/Checkbox"
    component={Checkbox}
    decorators={[
        Story => (
            <ThemeProvider defaultIsDarkModeEnabled={useDarkMode()}>
                <Story />
            </ThemeProvider>
        ),
    ]}
    parameters={{
        viewMode: "docs",
        previewTabs: {
            canvas: { hidden: true },
        },
    }}
/>

# Checkbox

This is a documentation of `<Checkbox/>` component.
The checkbox allow the user to select one or more options from a list.
They are used to make multiple selections (from 0 to N elements) or to allow a binary choice,
when the user can select or deselect a single option.

-   [Structure](#structure)
-   [States](#sizes)
-   [Options](#options)
-   [Properties](#properties)

## Structure

Checkbox is made up of the following elements:

-   a button / box - mandatory.
-   a label, associated with the button - mandatory.
-   an additional text for the buttons / labels - optional.
-   a title, describing the context of the button group - mandatory for groups of boxes.
-   an additional description for the legend - optional.
-   an error message - required if a change of state is to be notified to the user.

## States

### Default

<Canvas>
    <Story name="Default">
        {[false, true].map(dark => (
            <Fragment key={dark}>
                <ThemeProvider dark={dark}></ThemeProvider>
            </Fragment>
        ))}
    </Story>
</Canvas>

### Disabled

<Canvas>
    <Story name="Disabled">
        {[false, true].map(dark => (
            <Fragment key={dark}>
                <ThemeProvider dark={dark}></ThemeProvider>
            </Fragment>
        ))}
    </Story>
</Canvas>

### Error

Option to update

<Canvas>
    <Story name="Error">
        {[false, true].map(dark => (
            <Fragment key={dark}>
                <ThemeProvider dark={dark}></ThemeProvider>
            </Fragment>
        ))}
    </Story>
</Canvas>

## Options

### Lists

Option to update

### Help text

<Canvas>
    <Story name="Help text">
        {[false, true].map(dark => (
            <Fragment key={dark}>
                <ThemeProvider dark={dark}></ThemeProvider>
            </Fragment>
        ))}
    </Story>
</Canvas>

## Properties

<ArgsTable of={Checkbox} />
